name: Build GUI

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      upload_release:
        description: 'Upload artifacts to release for the selected tag'
        required: false
        default: false
        type: boolean

jobs:
  build-gui:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: Windows-x64
            artifact: MkPFS-Windows-x64.exe
          - os: macos-latest
            target: macOS-arm64
            artifact: MkPFS-macOS-arm64.zip
          - os: macos-15-intel
            target: macOS-x64
            artifact: MkPFS-macOS-x64.zip
          - os: ubuntu-latest
            target: Linux-x64
            artifact: MkPFS-Linux-x64.AppImage

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Sync dependencies
        run: uv sync --frozen

      # ------------------------------------------------------------------ #
      # Windows: single-file executable
      # ------------------------------------------------------------------ #
      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          uv run pyinstaller \
            --name MkPFS \
            --onefile \
            --windowed \
            --icon assets/icon.ico \
            --hidden-import mkpfs.gui \
            --hidden-import tkinter \
            --collect-data tkinter \
            --collect-binaries tkinter \
            --collect-data customtkinter \
            --distpath dist-gui \
            scripts/gui-entry.py

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Move-Item -Path "dist-gui/MkPFS.exe" -Destination "${{ matrix.artifact }}"
          echo "artifact_name=${{ matrix.artifact }}" >> $env:GITHUB_ENV

      - name: Smoke test executable (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          & ".\${{ matrix.artifact }}" --smoke-test

      # ------------------------------------------------------------------ #
      # macOS: .app bundle with optional code signing
      # ------------------------------------------------------------------ #
      - name: Convert icon to ICNS (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          mkdir -p icon.iconset
          sips -z 16 16   assets/icon.png --out icon.iconset/icon_16x16.png
          sips -z 32 32   assets/icon.png --out icon.iconset/icon_16x16@2x.png
          sips -z 32 32   assets/icon.png --out icon.iconset/icon_32x32.png
          sips -z 64 64   assets/icon.png --out icon.iconset/icon_32x32@2x.png
          sips -z 128 128 assets/icon.png --out icon.iconset/icon_128x128.png
          sips -z 256 256 assets/icon.png --out icon.iconset/icon_128x128@2x.png
          sips -z 256 256 assets/icon.png --out icon.iconset/icon_256x256.png
          sips -z 512 512 assets/icon.png --out icon.iconset/icon_256x256@2x.png
          iconutil -c icns icon.iconset -o assets/icon.icns

      - name: Build (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          uv run pyinstaller \
            --name MkPFS \
            --windowed \
            --osx-bundle-identifier com.psbrew.mkpfs \
            --icon assets/icon.icns \
            --hidden-import mkpfs.gui \
            --hidden-import tkinter \
            --collect-data tkinter \
            --collect-binaries tkinter \
            --collect-data customtkinter \
            --distpath dist-gui \
            scripts/gui-entry.py

      - name: Smoke test executable (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          "dist-gui/MkPFS.app/Contents/MacOS/MkPFS" --smoke-test

      - name: Sign (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          codesign --deep --force --sign - "dist-gui/MkPFS.app"
          xattr -d -r com.apple.quarantine "dist-gui/MkPFS.app" 2>/dev/null || true
          echo "Ad-hoc signed dist-gui/MkPFS.app"

      - name: Package (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          ditto -c -k --sequesterRsrc --keepParent "dist-gui/MkPFS.app" "${{ matrix.artifact }}"
          echo "artifact_name=${{ matrix.artifact }}" >> "$GITHUB_ENV"

      # ------------------------------------------------------------------ #
      # Linux: single binary wrapped as AppImage
      # ------------------------------------------------------------------ #
      - name: Build (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          uv run pyinstaller \
            --name MkPFS \
            --onefile \
            --windowed \
            --hidden-import mkpfs.gui \
            --hidden-import tkinter \
            --collect-data tkinter \
            --collect-binaries tkinter \
            --collect-data customtkinter \
            --distpath dist-gui \
            scripts/gui-entry.py

      - name: Smoke test executable (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          dist-gui/MkPFS --smoke-test

      - name: Package AppImage (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libfuse2

          binary="dist-gui/MkPFS"
          appdir="MkPFS.AppDir"
          mkdir -p "$appdir/usr/bin"
          cp "$binary" "$appdir/usr/bin/MkPFS"
          chmod +x "$appdir/usr/bin/MkPFS"

          printf '%s\n' '[Desktop Entry]' 'Name=MkPFS' 'Exec=MkPFS' 'Icon=mkpfs' \
            'Type=Application' 'Categories=Utility;' 'Comment=PlayStation File System Builder' \
            > "$appdir/MkPFS.desktop"

          cp assets/icon.png "$appdir/mkpfs.png"

          wget -q "https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage" -O linuxdeploy
          chmod +x linuxdeploy
          ./linuxdeploy --appdir "$appdir" --output appimage

          mv MkPFS-x86_64.AppImage "${{ matrix.artifact }}"
          echo "artifact_name=${{ matrix.artifact }}" >> "$GITHUB_ENV"

      - name: Generate checksum
        run: |
          uv run python -c "from pathlib import Path; import hashlib; path = Path('${{ env.artifact_name }}'); digest = hashlib.sha256(path.read_bytes()).hexdigest(); Path(str(path) + '.sha256').write_text(f'{digest}  {path.name}\n')"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: |
            ${{ env.artifact_name }}
            ${{ env.artifact_name }}.sha256

  release:
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && inputs.upload_release && startsWith(github.ref, 'refs/tags/v'))
    needs: build-gui
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts/
          merge-multiple: true

      - name: Generate release notes from commits
        id: notes
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          prev=$(git describe --tags --abbrev=0 "${tag}^" 2>/dev/null || echo "")
          {
            echo "## Changes"
            echo ""
            if [ -z "$prev" ]; then
              # First release: limit to last 30 days to avoid dumping entire upstream history
              git log --pretty=format:'- %s' --no-merges --since="30 days ago" | awk '!seen[$0]++'
            else
              git log --pretty=format:'- %s' --no-merges "${prev}..${tag}" | awk '!seen[$0]++'
            fi
          } > release_notes.md
          echo "body_path=release_notes.md" >> "$GITHUB_OUTPUT"

      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          body_path: ${{ steps.notes.outputs.body_path }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
